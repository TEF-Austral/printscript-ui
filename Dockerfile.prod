FROM node:20-alpine
WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .
RUN npm run build

ARG PORT=4173
ENV PORT=$PORT
EXPOSE $PORT

CMD ["sh", "-c", "npm run preview -- --host 0.0.0.0 --port $PORT"]